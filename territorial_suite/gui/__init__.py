"""GUI layer: docks, dialogs and map tools.

The GUI is an *adapter*: it collects user choices, starts background tasks and applies the
results to the project. It contains no domain logic - everything it does is available from
Processing as well.
"""
